apiVersion: v2
name: taugrid-core
description: |
  taugrid-core: Stellar + prewarm + optional ResourceFlavors + namespaces. The
  first-party Tau-owned bits of a TauGrid-on-AKS install.

  Assumes Kueue, kuberay-operator, and AKS managed GPU readiness are already
  present. See ../INSTALL.md for the recommended install order.
type: application
# TauGrid-owned charts and images share one public release version.
version: 0.3.0
# The tau and taugrid-portal image release tags follow appVersion.
appVersion: "0.3.0"
keywords: [taugrid, ai, kueue, multi-cloud]
maintainers:
  - name: taugrid
    email: taugrid@example.invalid
