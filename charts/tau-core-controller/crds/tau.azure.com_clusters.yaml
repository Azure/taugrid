---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: clusters.tau.azure.com
spec:
  group: tau.azure.com
  names:
    kind: TauCluster
    listKind: TauClusterList
    plural: clusters
    shortNames:
    - tc
    singular: cluster
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.managementMode
      name: Mode
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            default: {}
            properties:
              deletionPolicy:
                default: Retain
                description: DeletionPolicy defaults to retaining every managed cluster
                  resource.
                enum:
                - Retain
                - DeleteManaged
                type: string
              features:
                default: {}
                properties:
                  multiKueue:
                    default: Disabled
                    enum:
                    - Disabled
                    - Beta
                    type: string
                type: object
              managementMode:
                default: Observe
                description: |-
                  ManagementMode controls whether TauCluster only reports desired-state
                  differences or is allowed to reconcile explicitly owned resources.
                enum:
                - Observe
                - Reconcile
                type: string
              nodes:
                properties:
                  labelRules:
                    items:
                      properties:
                        labels:
                          additionalProperties:
                            type: string
                          description: |-
                            Labels is the exact set of node label keys and values this rule desires.
                            The controller never owns or removes keys not declared here.
                          minProperties: 1
                          type: object
                        match:
                          properties:
                            vmSizes:
                              description: |-
                                VMSizes limits a rule to nodes whose AKS VM-size label matches one of
                                these values. An empty list matches every node selected by Nodes.Selector.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: set
                          type: object
                      required:
                      - labels
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  selector:
                    additionalProperties:
                      type: string
                    description: Selector limits node discovery to labels that match
                      every key/value pair.
                    type: object
                type: object
              queues:
                default: {}
                properties:
                  clusterQueues:
                    items:
                      properties:
                        name:
                          description: Name is the name of a cluster-scoped object.
                          minLength: 1
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  ownership:
                    default: External
                    description: |-
                      Ownership controls whether queue objects are observed, explicitly
                      adopted, or managed. External is the safe default for GitOps objects.
                    enum:
                    - External
                    - Adopt
                    - Manage
                    type: string
                  resourceFlavors:
                    items:
                      properties:
                        name:
                          description: Name is the name of a cluster-scoped object.
                          minLength: 1
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  sharedLocalQueues:
                    items:
                      properties:
                        name:
                          description: Name is the name of the referenced object.
                          minLength: 1
                          type: string
                        namespace:
                          description: Namespace is the namespace containing the referenced
                            object.
                          minLength: 1
                          type: string
                      required:
                      - name
                      - namespace
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - namespace
                    - name
                    x-kubernetes-list-type: map
                  topology:
                    properties:
                      name:
                        description: Name is the name of a cluster-scoped object.
                        minLength: 1
                        type: string
                    required:
                    - name
                    type: object
                type: object
              workloadProfiles:
                description: |-
                  WorkloadProfiles declares stable workload intent. Live quota, capacity,
                  flavor selectors, and topology selectors are resolved into status instead.
                items:
                  description: |-
                    WorkloadProfile is stable workload intent. It deliberately excludes observed
                    capacity, quota, flavor selectors, and topology selectors.
                  properties:
                    applicability:
                      properties:
                        lanes:
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        namespaces:
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        teams:
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                      type: object
                    defaultLocalQueue:
                      minLength: 1
                      type: string
                    description:
                      type: string
                    executionTarget:
                      default: singleCluster
                      description: |-
                        ExecutionTarget selects whether a profile executes on the submitting cluster
                        or through the explicitly gated MultiKueue Beta path.
                      enum:
                      - singleCluster
                      - multiKueueBeta
                      type: string
                    gpusPerWorker:
                      format: int32
                      minimum: 0
                      type: integer
                    mode:
                      enum:
                      - fixed
                      - elastic
                      type: string
                    name:
                      pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                      type: string
                    placement:
                      enum:
                      - independent
                      - single-node-nvlink
                      - multi-node-nccl
                      type: string
                    priorities:
                      properties:
                        disableDefaultPriorities:
                          description: |-
                            DisableDefaultPriorities explicitly permits both priority class references
                            to be absent.
                          type: boolean
                        podPriorityClassName:
                          type: string
                        workloadPriorityClassName:
                          type: string
                      type: object
                      x-kubernetes-validations:
                      - message: priority class names are required unless default
                          priorities are explicitly disabled
                        rule: '(has(self.disableDefaultPriorities) && self.disableDefaultPriorities)
                          ? (!has(self.workloadPriorityClassName) && !has(self.podPriorityClassName))
                          : (has(self.workloadPriorityClassName) && size(self.workloadPriorityClassName)
                          > 0 && has(self.podPriorityClassName) && size(self.podPriorityClassName)
                          > 0)'
                    workerCount:
                      format: int32
                      minimum: 1
                      type: integer
                  required:
                  - defaultLocalQueue
                  - gpusPerWorker
                  - mode
                  - name
                  - placement
                  - priorities
                  - workerCount
                  type: object
                  x-kubernetes-validations:
                  - message: workerCount greater than one requires placement=multi-node-nccl
                    rule: self.workerCount == 1 || self.placement == 'multi-node-nccl'
                  - message: multiKueueBeta requires non-empty team and namespace
                      applicability allowlists
                    rule: self.executionTarget != 'multiKueueBeta' || (has(self.applicability)
                      && has(self.applicability.teams) && size(self.applicability.teams)
                      > 0 && has(self.applicability.namespaces) && size(self.applicability.namespaces)
                      > 0)
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              workspaceDefaults:
                default: {}
                properties:
                  defaultQueue:
                    default: jobqueue
                    type: string
                type: object
            type: object
          status:
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              desiredStateHash:
                type: string
              managedResources:
                items:
                  properties:
                    apiVersion:
                      type: string
                    kind:
                      type: string
                    name:
                      type: string
                    namespace:
                      type: string
                    ownership:
                      enum:
                      - External
                      - Adopt
                      - Manage
                      type: string
                    uid:
                      type: string
                  required:
                  - apiVersion
                  - kind
                  - name
                  - ownership
                  type: object
                type: array
                x-kubernetes-list-type: atomic
              nodes:
                properties:
                  drifted:
                    format: int32
                    type: integer
                  observed:
                    format: int32
                    type: integer
                  ready:
                    format: int32
                    type: integer
                type: object
              observedGeneration:
                format: int64
                type: integer
              phase:
                enum:
                - Pending
                - Ready
                - Degraded
                type: string
              queues:
                properties:
                  drifted:
                    format: int32
                    type: integer
                  observed:
                    format: int32
                    type: integer
                  ready:
                    format: int32
                    type: integer
                type: object
              workloadProfiles:
                description: |-
                  ProfileSetStatus is the ready-or-drifted resolved state for a workload
                  profile declaration set.
                properties:
                  drifted:
                    format: int32
                    type: integer
                  observed:
                    format: int32
                    type: integer
                  observedGeneration:
                    format: int64
                    type: integer
                  profileSetHash:
                    type: string
                  profiles:
                    items:
                      description: |-
                        ResolvedWorkloadProfile combines normalized stable intent with only the
                        observed resource identities needed by renderers and diagnostics.
                      properties:
                        applicability:
                          properties:
                            lanes:
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: set
                            namespaces:
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: set
                            teams:
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: set
                          type: object
                        clusterQueues:
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        conditions:
                          items:
                            description: Condition contains details for one aspect
                              of the current state of this API Resource.
                            properties:
                              lastTransitionTime:
                                description: |-
                                  lastTransitionTime is the last time the condition transitioned from one status to another.
                                  This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                                format: date-time
                                type: string
                              message:
                                description: |-
                                  message is a human readable message indicating details about the transition.
                                  This may be an empty string.
                                maxLength: 32768
                                type: string
                              observedGeneration:
                                description: |-
                                  observedGeneration represents the .metadata.generation that the condition was set based upon.
                                  For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                                  with respect to the current state of the instance.
                                format: int64
                                minimum: 0
                                type: integer
                              reason:
                                description: |-
                                  reason contains a programmatic identifier indicating the reason for the condition's last transition.
                                  Producers of specific condition types may define expected values and meanings for this field,
                                  and whether the values are considered a guaranteed API.
                                  The value should be a CamelCase string.
                                  This field may not be empty.
                                maxLength: 1024
                                minLength: 1
                                pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                                type: string
                              status:
                                description: status of the condition, one of True,
                                  False, Unknown.
                                enum:
                                - "True"
                                - "False"
                                - Unknown
                                type: string
                              type:
                                description: type of condition in CamelCase or in
                                  foo.example.com/CamelCase.
                                maxLength: 316
                                pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                                type: string
                            required:
                            - lastTransitionTime
                            - message
                            - reason
                            - status
                            - type
                            type: object
                          type: array
                          x-kubernetes-list-map-keys:
                          - type
                          x-kubernetes-list-type: map
                        defaultLocalQueue:
                          minLength: 1
                          type: string
                        description:
                          type: string
                        executionTarget:
                          default: singleCluster
                          description: |-
                            ExecutionTarget selects whether a profile executes on the submitting cluster
                            or through the explicitly gated MultiKueue Beta path.
                          enum:
                          - singleCluster
                          - multiKueueBeta
                          type: string
                        gpusPerWorker:
                          format: int32
                          minimum: 0
                          type: integer
                        localQueues:
                          items:
                            description: ResolvedLocalQueue identifies an observed
                              LocalQueue and its ClusterQueue.
                            properties:
                              clusterQueue:
                                type: string
                              name:
                                type: string
                              namespace:
                                type: string
                            required:
                            - clusterQueue
                            - name
                            - namespace
                            type: object
                          type: array
                          x-kubernetes-list-map-keys:
                          - namespace
                          - name
                          x-kubernetes-list-type: map
                        mode:
                          enum:
                          - fixed
                          - elastic
                          type: string
                        name:
                          pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                          type: string
                        placement:
                          enum:
                          - independent
                          - single-node-nvlink
                          - multi-node-nccl
                          type: string
                        podPriorityClasses:
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        priorities:
                          properties:
                            disableDefaultPriorities:
                              description: |-
                                DisableDefaultPriorities explicitly permits both priority class references
                                to be absent.
                              type: boolean
                            podPriorityClassName:
                              type: string
                            workloadPriorityClassName:
                              type: string
                          type: object
                          x-kubernetes-validations:
                          - message: priority class names are required unless default
                              priorities are explicitly disabled
                            rule: '(has(self.disableDefaultPriorities) && self.disableDefaultPriorities)
                              ? (!has(self.workloadPriorityClassName) && !has(self.podPriorityClassName))
                              : (has(self.workloadPriorityClassName) && size(self.workloadPriorityClassName)
                              > 0 && has(self.podPriorityClassName) && size(self.podPriorityClassName)
                              > 0)'
                        resourceFlavors:
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        topologies:
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        workerCount:
                          format: int32
                          minimum: 1
                          type: integer
                        workloadPriorityClasses:
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                      required:
                      - defaultLocalQueue
                      - gpusPerWorker
                      - mode
                      - name
                      - placement
                      - priorities
                      - workerCount
                      type: object
                      x-kubernetes-validations:
                      - message: workerCount greater than one requires placement=multi-node-nccl
                        rule: self.workerCount == 1 || self.placement == 'multi-node-nccl'
                      - message: multiKueueBeta requires non-empty team and namespace
                          applicability allowlists
                        rule: self.executionTarget != 'multiKueueBeta' || (has(self.applicability)
                          && has(self.applicability.teams) && size(self.applicability.teams)
                          > 0 && has(self.applicability.namespaces) && size(self.applicability.namespaces)
                          > 0)
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  ready:
                    format: int32
                    type: integer
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
